# uv pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

fastapi
uvicorn[standard]
websockets
tqdm
datasets
python-multipart

# AI agent — provider-agnostic LLM clients (see backend/agent/)
openai
anthropic
httpx

# Dev: execution tracing / call-graph profiling (viztracer -o trace.json; vizviewer trace.json)
viztracer